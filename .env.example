# Copy to .env and fill in. Never commit .env.

# --- LLM Providers (choose one) ---
# FLock (required for hackathon submission)
# FLOCK_API_KEY=sk-your-flock-api-key

# OpenAI (optional, for local development)
# OPENAI_API_KEY=sk-your-openai-api-key

# --- Agent (used by score.js) ---
# AGENT_LABEL=krumpbot-fit

# --- Canton (Daml ledger for session logs) ---
# CANTON_ENABLE=true
# CANTON_JSON_API_BASE_URL=http://localhost:7575
# CANTON_LEDGER_ID=sandbox
# CANTON_PATIENT_PARTY=sandbox::1220...
# CANTON_PHYSIO_PARTY=sandbox::1220...
# CANTON_JWT=<from: node canton/jwt-dev.js>
# CANTON_SESSIONLOG_TEMPLATE_ID=<optional; set if you rebuild the DAR>

# --- Optional: Anyway (LLM observability) ---
# ANYWAY_API_KEY=your-anyway-api-key

# --- Optional: Stripe (payments) ---
# STRIPE_SECRET_KEY=sk_test_... or sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...   # if using webhooks

# --- Optional: OpenClaw web_search (Kimi/Moonshot; set where gateway runs) ---
# KIMI_API_KEY=...   # or MOONSHOT_API_KEY=...  for web_search (FLock has Kimi models)

# --- Optional: ElevenLabs (voice + music for video bot; Option B) ---
# ELEVENLABS_API_KEY=...   # from https://elevenlabs.io/app/settings/api-keys
# ELEVENLABS_VOICE_ID=...   # optional; default voice used for TTS
# ELEVENLABS_TTS_MODEL_ID=eleven_v3   # 70+ languages (default). Or eleven_turbo_v2_5 for lower latency (32 langs)
# ELEVENLABS_STT_MODEL_ID=scribe_v2   # 90+ languages, auto-detect; optional language_code from Telegram improves accuracy
# ELEVENLABS_MUSIC_AFTER_ANALYSIS=1   # set to 1/true to send a short beat after each video analysis
