# Quantum exercise optimisation (Guppy + Selene)
# Requires Python 3.10+. Upgrade pip first: pip install --upgrade pip
# Then: python3 -m venv .venv-quantum && source .venv-quantum/bin/activate && pip install -r quantum/requirements.txt
guppylang
selene-sim
